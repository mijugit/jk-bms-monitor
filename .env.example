# Copy this file to .env and fill in your values.
# .env is listed in .gitignore and must NEVER be committed.

APP_ENV=development
APP_URL=http://localhost
# Shared password gating the whole web app (Access Control — MVP has one user, no accounts).
JKBMS_PASS=change-me

DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=jkbms_db
DB_USER=root
DB_PASS=
